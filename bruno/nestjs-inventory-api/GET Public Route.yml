info:
  name: GET Public Route
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3005/inventory
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer test-jwt-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
