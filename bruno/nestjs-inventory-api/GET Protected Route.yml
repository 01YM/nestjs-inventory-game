info:
  name: GET Protected Route
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3005/inventory/protected
  headers:
    - name: Authorization
      value: Bearer test-jwt-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
